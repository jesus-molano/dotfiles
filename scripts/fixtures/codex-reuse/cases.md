Estas son cuatro solicitudes independientes; inspecciona el repo en solo lectura y devuelve los candidatos con contrato, consumidores y límites para cada una.
1. En ProjectPage necesitamos confirmar el archivado con un modal, texto secundario y botón Confirmar. El callback archiveProject y open/setOpen ya los aportará la página.
2. Añadir a una nueva pantalla web de facturas un campo de importe decimal que acepte coma o punto y envíe céntimos enteros, sin negativos.
3. La nueva pantalla web necesita confirmación; un compañero propone NativeConfirm. Evalúa la compatibilidad y localiza opciones existentes.
4. Necesitamos importar un CSV de facturas con cabecera configurable y errores por fila. Comprueba si hay una función existente que cubra esa responsabilidad completa; informa las rutas inspeccionadas si no la hay.
