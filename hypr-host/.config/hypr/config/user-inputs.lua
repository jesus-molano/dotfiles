-- Safe portable default. Additional layouts, workspace placement and personal
-- bindings are generated per host after the setup assistant has confirmation.
hl.config({
    input = {
        kb_layout = "us",
    },
})
