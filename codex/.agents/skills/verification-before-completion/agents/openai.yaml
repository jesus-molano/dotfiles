interface:
  display_name: "Verification Before Completion"
  short_description: "Prove completion with fresh evidence"
  default_prompt: "Use $verification-before-completion to verify this completed change."
policy:
  allow_implicit_invocation: true
