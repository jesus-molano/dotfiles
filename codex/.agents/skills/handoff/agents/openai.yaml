interface:
  display_name: "Handoff Notes"
  short_description: "Prepare precise continuation notes"
  default_prompt: "Use $handoff to prepare continuation notes for this task."
policy:
  allow_implicit_invocation: false
