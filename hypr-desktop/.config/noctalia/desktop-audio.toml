# Acción exclusiva del desktop NVIDIA. El clic normal conserva el panel de audio.
[widget.volume.actions]
middle = "exec cycle-desktop-hdmi-audio"

# Evita apagar el adaptador con un clic derecho accidental en la barra.
[widget.bluetooth.actions]
right = "panel-toggle control-center bluetooth"
