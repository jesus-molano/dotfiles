-- Spanish layout for the physical Keychron K2. Kanata only remaps Caps Lock;
-- every other key keeps this layout.
TERMINAL = "ghostty"
FILE_MANAGER = "dolphin"
BROWSER = "qutebrowser"
EDITOR = "ghostty -e nvim"

hl.config({
    input = {
        kb_layout = "es",
    },
})
